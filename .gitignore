.DS_Store

# Figure PNGs in static/images/ and video clips in static/videos/ ARE committed
# (actual sizes stayed small — a few hundred KB per image, ~5-13MB per clip).
# Only the vector PDF sources for figures are excluded — keep those locally
# for provenance only.
static/images/*.pdf

# Scratch/rejected clips, not for the site
static/videos/trash/
