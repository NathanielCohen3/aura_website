.DS_Store

# Video/image assets are hosted externally, not committed to this repo
# (GitHub Pages soft limit is ~1GB and doesn't serve Git LFS content properly).
# Drop files here for local preview only; host the real files externally
# and point index.html's <source src> / <img src> at those URLs.
static/videos/
static/images/
