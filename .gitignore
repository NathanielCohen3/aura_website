.DS_Store

# Video assets: still deciding hosting strategy (see project notes) — not
# committed yet. Drop files here for local preview; host externally later
# and point index.html's <source src> at those URLs.
static/videos/

# Figure PNGs in static/images/ ARE committed (small, a few hundred KB each).
# Their vector PDF sources are not — keep those locally for provenance only.
static/images/*.pdf
